import { Grid } from '@mui/material';

const ParametersSelectionComponent = () => {
    return (
        <Grid
            item
            xs={4}
            sx={{ backgroundColor: 'green', height: '100%' }}
        ></Grid>
    );
};

export default ParametersSelectionComponent;
